apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  # "namespace" omitted since ClusterRoles are not namespaced
  name: huey-read-write
rules:
- apiGroups: [""]
  resources: ["pod", "nodes"]
  verbs: ["get", "watch", "list"]