#apiVersion: v1
#kind: PersistentVolume
#metadata:
#  name: mysql-pv-volume
#  labels:
#    type: mysql-pv
#spec:
#  storageClassName: manual
#  capacity:
#    storage: 2Gi
#  accessModes:
#    - ReadWriteOnce
#  hostPath:
#    path: "/mnt/data/mysql"

#This is to use static provisioning with manual created EBS
apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-ebs-restore
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  # Use the EBS volume ID here
  csi:
    driver: ebs.csi.aws.com
    volumeHandle: vol-09e559f9a5cf91821
    fsType: xfs
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: topology.ebs.csi.aws.com/zone
          operator: In
          values:
          - ap-southeast-2a
